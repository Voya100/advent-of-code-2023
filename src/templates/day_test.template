import { part1, part2 } from './day__DAY__';

const input = ``;

describe('day __DAY__, part 1', () => {
  it('should work with test input', () => {
    expect(part1(input)).toBe(null);
  });
});

describe('day __DAY__, part 2', () => {
  it('should work with test input', () => {
    expect(part2(input)).toBe(null);
  });
});